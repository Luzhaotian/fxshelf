# Starfield — 动效说明（大白话）

就是一层会动的星空背景：星星从远处朝你飞过来，像坐飞船穿星海。

- 不用 Three.js，也不用 particles.js
- 自己用 Canvas 画小点点 + 拖尾
- 偶尔闪一下更像真星星
- 可以包一层内容，文字按钮叠在星空上面

调参大致直觉：

- `particleCount` 越大星星越多
- `speed` 越大飞得越快
- `brightness` 越大越亮
- `glitterIntensity` 越大闪得越狠
- `trailAmount` 越大尾巴越长
